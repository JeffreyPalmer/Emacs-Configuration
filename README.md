This is just a github version of my emacs configuration, for my sanity. :)

This now uses Cask and Pallet to manage packages.

Getting started with a new checkout:
```
brew install cask
cd ~/.emacs.d
cask install
```
